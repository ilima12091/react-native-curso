import { tv } from 'tailwind-variants';

export const inputStyles = tv({
  base: 'border border-gray-300 rounded p-4 color-text active:border-primary focus:border-primary placeholder:color-text',
});
