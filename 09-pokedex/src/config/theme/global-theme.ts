import { StyleSheet } from 'react-native';

export const globalTheme = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 16,
  },
  fab: {
    position: 'absolute',
    right: 16,
    bottom: 16,
  },
});
